"use client";

/**
 * ChildSelector — Dropdown to switch between children in the sidebar.
 * Shows child name, age, and avatar. Includes "Add child" option.
 *
 * TODO: Implement with Supabase children table query.
 * See GitHub Issue #4 for requirements.
 */
export function ChildSelector() {
  return (
    <div className="rounded-lg border border-dashed p-3 text-center text-sm text-muted-foreground">
      {/* TODO: Replace with real child selector */}
      Child selector — see Issue #4
    </div>
  );
}
